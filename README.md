# unix-cli-emulator

Вариант 5

Эмулятор unix-оболочки на Python. Есть своя VFS и CLI с базовыми командами (`ls`, `cd`, `exit`).

## Запуск

```bash
./run.sh
```

или вручную: `cd src && python3 main.py`
# unix-cli-emulator
# unix-cli-emulator
